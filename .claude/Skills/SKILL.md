---
name: mermaid-syntax
description: 將自然語言描述的流程、架構、狀態轉換、資料庫結構轉換成正確的 Mermaid 圖表語法。當需要判斷該用哪種 Mermaid 圖表類型、或需要查詢特定圖表語法規則與常見錯誤時使用此技能。
---

# Mermaid 語法轉換技能

這份技能提供「描述 → Mermaid 語法」的判斷邏輯，分成三部分：圖表類型決策樹、各類型語法速查、常見錯誤與避免方式。

## 一、圖表類型決策樹

依描述的性質，依序判斷：

1. **描述包含「先後順序」「條件分支」「決策點」** → `flowchart`
2. **描述是「誰跟誰互動、呼叫順序、訊息傳遞」（例如 API 呼叫序列、使用者與系統互動）** → `sequenceDiagram`
3. **描述是「某個東西的狀態怎麼轉換」（例如訂單狀態、任務生命週期）** → `stateDiagram-v2`
4. **描述是「資料表結構與關聯」** → `erDiagram`
5. **描述是「類別/物件的屬性與關係」（傳統 UML）** → `classDiagram`
6. **描述是「發散式的主題與子主題，無明確先後順序」** → `mindmap`
7. **描述包含「時間軸、排程、里程碑」** → `gantt`
8. **描述是「巢狀包含關係」（東西裝在東西裡面，如系統/子系統）** → `flowchart` + `subgraph`（Mermaid 沒有專門的巢狀結構圖類型，用 subgraph 模擬容器）

若描述同時符合多種類型（例如「畫出這個系統的架構，並說明資料怎麼流動」），優先問呼叫方要哪一種，或依主要意圖選一種為主、在說明文字中提及可另外產出其他視角。

## 二、各類型語法速查

### Flowchart（最常用）
```
flowchart TD
  A[開始] --> B{條件判斷}
  B -->|是| C[執行動作]
  B -->|否| D[結束]
  C --> D
```
- 方向：`TD`（上到下）、`LR`（左到右）、`BT`、`RL`
- 節點形狀：`[矩形]`、`(圓角)`、`{菱形/決策}`、`((圓形))`、`[[子程序]]`
- 巢狀分組用 `subgraph 名稱 ... end`

### Sequence Diagram
```
sequenceDiagram
  participant U as 使用者
  participant S as 系統
  U->>S: 送出請求
  S-->>U: 回傳結果
```
- `->>` 實線箭頭（同步呼叫）、`-->>` 虛線箭頭（回應/非同步）
- `activate`/`deactivate` 標示生命週期

### State Diagram
```
stateDiagram-v2
  [*] --> 待處理
  待處理 --> 執行中: 開始
  執行中 --> 完成: 結束
  完成 --> [*]
```

### ER Diagram
```
erDiagram
  USERS ||--o{ ORDERS : places
  USERS {
    uuid id PK
    string email
  }
```
- 關聯符號：`||--o{`（一對多）、`||--||`（一對一）、`}o--o{`（多對多）

### Mindmap
```
mindmap
  root((主題))
    子主題一
      細項A
      細項B
    子主題二
```

### Gantt
```
gantt
  title 專案時程
  section 階段一
  任務A :a1, 2026-08-01, 7d
  任務B :after a1, 5d
```

## 三、常見錯誤與避免方式

1. **節點文字含特殊符號（括號、冒號、引號）未加雙引號** → 會導致解析失敗，例如 `A[說明: 這是重點]` 應寫成 `A["說明: 這是重點"]`
2. **中文字元本身沒問題，但節點 ID 不可用中文** → 節點 ID（如 A、B、Node1）用英數字，顯示文字才放中文：`A[開始流程]` 正確，`開始[開始流程]` 容易出錯
3. **subgraph 內外的連線方向混亂** → 盡量讓同一 subgraph 內的節點先定義完，再寫跨 subgraph 的連線，避免 Mermaid 誤判巢狀範圍
4. **一張圖塞太多節點導致難以閱讀** → 超過 15-20 個節點時，考慮拆成多張圖或用 subgraph 分組
5. **忘記圖表類型宣告行** → 每份 Mermaid 語法第一行必須是圖表類型宣告（如 `flowchart TD`），漏寫會整份無法渲染

## 四、輸出前檢查清單

產出語法前確認：
- [ ] 第一行有正確的圖表類型宣告
- [ ] 所有含特殊符號的節點文字都加了雙引號
- [ ] 節點 ID 為英數字，顯示文字可為中文
- [ ] 箭頭方向與描述的邏輯順序一致
- [ ] 節點數量合理（建議 20 個以內），過多時已拆分或分組
