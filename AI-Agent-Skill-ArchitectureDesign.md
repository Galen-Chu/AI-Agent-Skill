# AI Agent／Skill 架構設計方法論

這份文件記錄的是**設計方法論本身**，不是特定專案的實作清單（實作清單見 `INDEX.md`，專案總覽見 `README.md`）。目的是把整個架構建立過程中確立的判斷框架抽出來，讓同一套邏輯能被複用到未來任何新的 Agent／Skill 開發專案，不用每次重新推導一次。

---

## 一、核心架構模型：四層架構

```
① 協調層 Orchestrator     → 判斷請求該找誰、要串幾個工作流
② 領域工作流層 Workflow    → 各自完整生命週期（規劃→執行→驗證→交付）
③ 能力模組層 Capability    → 單一職責、無領域判斷、被上層呼叫
④ 記憶/狀態層 Memory       → 排程執行之間的狀態延續（外部化，不寫在 Agent 定義內）
```

這套分層對應軟體工程裡「關注點分離」的通則，跟 CI/CD 的 `觸發（cron/webhook）` vs `執行邏輯（job）`、Serverless 的 `事件觸發` vs `function 本體`、傳統維運的 `crontab` vs `script` 是同構的概念，只是套用在 LLM Agent 架構上。

### 分層判斷標準

| 層級 | 判斷依據 | 反面訊號 |
|---|---|---|
| ① 協調層 | 不執行任何領域工作，只做路由與多 Agent 協作規劃 | 如果它自己動手做事，就不是純協調層 |
| ② 領域工作流 | 核心難度來自**需要領域判斷力**，有自己完整生命週期 | 如果邏輯換個領域也通用，該歸③ |
| ③ 能力模組 | 核心難度來自**步驟多、需要串接排序**，無領域判斷 | 如果判斷內容需要領域知識，不該歸③ |
| ④ 記憶/狀態層 | Agent 定義只寫「規則」，儲存體在外部 | 這層通常不該有自己的 Agent，是基礎設施 |

---

## 二、規劃流程：由上而下五步驟

當你要從「一堆想做的事」推導出該建幾個 Agent時，依序執行：

```
Step 1  畫出宏觀工作流地圖
        （橫跨所有預計要做的任務，畫出：輸入 → 處理判斷 → 輸出交付）

Step 2  在地圖上標出「職責節點」
        對每個節點問：「這個節點的判斷邏輯，跟其他節點是同一套、只是觸發時機不同？
                        還是判斷邏輯本質上不同？」

Step 3  依 Step 2 的答案決定「開發程度」
        同一套邏輯、只差觸發時機 → 不開新 Agent，共用 + initialPrompt 變體
        判斷邏輯本質不同           → 值得開一份獨立 Agent 文件

Step 4  只對「值得獨立」的節點，才逐一定義②③④⑤（角色/工具/記憶/輸出）
        （避免對每個節點都重新設計一次，浪費在不必要的顆粒度上）

Step 5  最後才配置①觸發層
        （觸發方式不影響 Agent 定義，本來就該最後決定）
```

### 端到端領域 vs 可重用階段模組

規劃時常見的分類誤區，是把「有完整生命週期的領域」跟「會被多個領域呼叫的階段」混在同一個清單裡。拆分方法：

| 類型 | 性質 | 判斷方式 |
|---|---|---|
| 端到端領域（Domain） | 有自己完整生命週期，本身就是完整的骨架實例 | 這件事換個場景還是「同一件完整的事」（如：設計、開發、數據分析） |
| 可重用階段模組（Reusable Stage） | 不是獨立生命週期，是會被多個領域工作流呼叫進去用的步驟 | 這件事會被不只一個領域重複用到（如：爬蟲、測試、視覺化呈現） |

判斷錯誤的代價：把「可重用階段模組」誤判成「端到端領域」各自獨立開發，會導致重複造輪子；反過來把「端到端領域」硬塞進通用模組，會讓判斷邏輯互相打架、行為不可預測。

---

## 三、命名與 Description 規範

### 命名前綴

| 層級 | 前綴 | 範例 |
|---|---|---|
| ①協調層 | `orch-` | `orch-main` |
| ②領域工作流 | `wf-` | `wf-dev`、`wf-data-academic` |
| ③能力模組 | `mod-` | `mod-web-scraper` |
| Skill | 不加前綴，語意導向命名 | `mermaid-syntax` |

Skill 不加前綴的原因：Skill 是可攜式知識，命名該描述「這是什麼知識」而非「屬於哪一層」，因為同一份 Skill 可能同時被②跟③呼叫，貼層級前綴反而誤導。

### Description 三段式公式

```
[做什麼功能]。[觸發情境/關鍵字]。[所屬層級與可重用範疇]。
```

第一段影響**自動路由準確度**（系統依此比對是否該委派此 Agent），第三段是給**未來維護者**看的定位註記，半年後回來看不用重新推理這支 Agent 在架構裡的位置。

隨 Agent 數量增加最常見的一致性風險：新加的 Agent description 寫得比舊的隨便，導致自動路由對新 Agent 判斷不準。每加一個新 Agent，回頭對照既有 description 的詳細程度是否一致。

---

## 四、Skill 與 Subagent 的分工原則

| 元件 | 角色 | 對應內容 |
|---|---|---|
| Subagent | 執行者——有自己的身分、工具權限、獨立上下文 | 角色定位、工作流程、輸出規範 |
| Skill | 程序性知識——教 Claude「怎麼做」這件具體的事 | 判斷邏輯、決策樹、規則細節、常見錯誤 |

**判斷標準**：Agent 的核心判斷邏輯如果複雜到值得完整規則手冊等級的說明（決策樹、語法對照表、評估框架），該拆成 Skill，不該全寫進 Agent body。拆開的理由：
1. **Token 效率**：Agent body 一律載入，Skill 是漸進式揭露、需要時才載入
2. **重用粒度更細**：Subagent 是「整套服務」，Skill 是「可攜式知識」，其他 Agent 需要同樣判斷邏輯時可以直接掛載，不需要整個獨立 subagent 被呼叫
3. **維護分離**：改判斷邏輯只動 Skill，改執行方式只動 Subagent，兩者互不影響

**已知限制**：Skill 探索目前僅掃描 `.claude/skills/` 頂層，不支援巢狀資料夾分類（`skills/data-analysis/xxx` 這種分類方式不會被發現）。每個 Skill 必須是 `.claude/skills/<skill-name>/SKILL.md` 的資料夾＋固定檔名格式，分類靠命名語意與索引文件，不靠實體資料夾。

**開源 Skill 處理原則**：不直接複製第三方 Skill 原始檔案（可能有內部路徑引用依賴），一律改寫成自己的版本後收錄。

---

## 五、觸發層決策框架

觸發層（何時何處執行）跟定義層（做什麼、怎麼做）該徹底解耦——**Agent 定義本身不該寫任何排程邏輯**，觸發方式在部署時才決定。

### 四問判斷法

依序問，答案會自然導向對的觸發層級：

1. **這任務依賴「正在進行的對話上下文」嗎？** → 是 → session 內（`/loop`）
2. **這任務需要「本機環境」（檔案系統、本機服務）嗎？** → 是 → 本機排程（Desktop）
3. **這任務「不能漏跑」，即使裝置離線？** → 是 → 雲端排程（Cloud Routine）
4. **這任務涉及機敏/私人資料，不想離開本機？** → 是 → 即使前面判斷像雲端，也該收斂回本機

這四題本質上在問同一件事的四個面向：**上下文依賴、環境依賴、可靠性需求、資料主權**——只要有一項是強依賴，就會把判斷拉回較「近」的層級。

**設計原則**：用「關注點導向」（先問任務本質依賴什麼）而非「工具導向」（先問工具能做什麼）來決定觸發層。前者的架構穩定，即使底層工具換了，判斷邏輯不用重寫；後者容易被工具的能力邊界綁架。

---

## 六、記憶/狀態層設計原則

### Build vs Buy

工作流程管理常見會拆成兩個子問題，各自該用不同方式解決：

| 子問題 | 本質 | 建議做法 |
|---|---|---|
| 狀態追蹤（有哪些 Agent、跑到哪、上次結果如何） | 結構化資料的持久儲存 | 用現有成熟平台（Notion 等），不要自己重造資料庫＋UI |
| 路由判斷（新請求該交給哪個 Agent） | 需要語意理解的判斷力 | 用①協調層 Agent 處理 |

平台已經解決得很好的問題（結構化儲存、跨裝置同步），沒必要重新發明；Agent 的價值該留給真正需要判斷力的地方。

### 多個外部工具的分工判斷

當有多個候選工具（如 Notion vs Obsidian）能存放狀態/內容時，用觸發層四問法的同一套邏輯來分工，核心是判斷「**本機依賴程度**」：

- 雲端原生、任何觸發層都連得到的工具 → 適合搭配 Cloud Routine 等不依賴本機的觸發方式
- 本質是本機服務（如透過 `127.0.0.1` 提供 API）的工具 → 只適合搭配本機觸發（`/loop`、Desktop 排程），跟 Cloud Routine 觸發的工作流會產生依賴衝突

**內容性質也是分工依據**：結構化的狀態追蹤（給系統/未來的你看）適合資料庫型工具；個人閱讀、雙向連結、長期典藏的內容（給你自己看）適合筆記型工具。兩者不互相替代。

### ④層本身通常不需要專屬 Agent

④層的本質是**基礎設施**，不是**行為**。除非有明確證據顯示至少兩個以上呼叫方需要重複用到同一套「狀態讀寫」邏輯，才值得拆成一個③能力模組（例如「讀寫登記簿」可以是一個通用模組），單一 Agent 的內部狀態需求（如某工作流自己的跨日去重記錄）直接用 Read/Write 處理即可，不需要為此新增 Agent。

---

## 七、風險與權限設計原則

**核心原則**：欄位複雜度要對應風險等級，不是每個 Agent 都該用滿進階權限欄位。

| 風險特徵 | 對應做法 |
|---|---|
| 低風險、高可逆（查詢、整理、草擬） | 用行為準則處理（如「先做後告知」），不需要進階權限欄位 |
| 高風險、不可逆（部署、刪除、對外溝通） | 先確認再執行；必要時搭配 `isolation: worktree` 隔離實驗性修改 |
| 排程觸發、無人值守 | 需要 `permissionMode: acceptEdits` 等欄位讓 Agent 能自主完成任務，因為沒有人能回應確認提示 |

用行為準則（寫在 Agent body）處理低風險情境的風險控管，用權限鎖（frontmatter 進階欄位）處理高風險或無人值守情境——不要每個 Agent 都套用最嚴格的權限管制，那是過度設計。

### 無人值守 Agent 的額外設計要求

排程觸發、沒有人在場的 Agent，需要額外具備：
1. **合理預設值，而非卡住等待確認**：遇到不確定情況要套用預設值並在輸出中註明，不能停在原地
2. **失敗誠實記錄，而非虛構完整結果**：任一環節失敗時要清楚寫明原因，不能為了「看起來完成」而蒙混

---

## 八、版權與內容合規原則

任何會蒐集、摘要、轉述外部內容的 Agent／Skill，合規規則應該收在**資料源頭**（如蒐集模組），而非依賴下游每個呼叫方各自處理——因為源頭沒守住，風險會擴散到所有下游呼叫方。核心規則：

- 內容一律改寫，不逐字複製
- 引用需嚴格限制字數與次數（單一來源最多一次簡短引用）
- 保留來源標註，供查證
- 受著作權保護的創作內容（歌詞、詩詞等）一律不重製

---

## 九、何時該正式化協調層

①協調層不需要一開始就存在。建議的時機判斷：

- **規模小時**：協調職責可以先隱含在某個互動式 Agent（如日常事務助理）身上，遇到超出範疇的請求時誠實告知即可，不需要獨立的協調 Agent
- **達到門檻後**（經驗參考值：Agent 數量到 5-8 個以上、單一 Agent 光判斷路由就開始佔用大量對話內容時）：拆成獨立的正式協調層 Agent，此時拆分才有實際效益

正式化後，協調層 Agent 的核心職責僅限於：
1. 讀取現有 Agent 目錄（索引文件），了解職責邊界
2. 判斷請求該交給單一 Agent，還是需要多 Agent 協作
3. 需要多 Agent 協作時規劃呼叫順序、整合結果
4. 保持路由邏輯透明，不黑盒處理

---

## 十、索引文件的雙軌設計

建議專案同時維護兩份文件，職責不同：

| 文件 | 定位 | 更新頻率 |
|---|---|---|
| `README.md` | 給第一次接觸專案的人看架構全貌與設計原則 | 架構有重大變動時更新 |
| `INDEX.md` | 給日常維護時查詢狀態、依賴關係、參考來源 | 每完成一個項目就更新 |

兩者都該跟 `.claude/` 一起版控，讓任何 clone 這個 repo 的人不用逐一打開每份 Agent／Skill 檔案，就能理解整體架構與目前進度。

---

## 十一、評測／驗收迴圈設計原則

當專案發展到有多個②工作流持續產出報告/內容，容易冒出一個念頭：「要不要建一個驗收機制，確保每次輸出品質可控？」常見的完整想像是一個四階段閉環：

```
1. 蒐集（規劃該評什麼指標） → 2. 設計（把指標訂成 Schema）
       ↑                                    ↓
4. 優化（產出檢討與修正指令） ← 3. 執行（依 Schema 填數據、產出報告）
```

這四階段本身沒有問題，但**一次到位建四個獨立 Agent 通常是過度設計**。拆解後會發現：

| 階段 | 本質 | 是否需要領域判斷 | 適合歸屬 |
|---|---|---|---|
| 1. 蒐集指標 | 決定「這類輸出該看什麼」 | 需要——不同領域（財報 vs 新聞摘要 vs 程式碼）該看的指標完全不同 | 呼叫方自己的職責，不該外包給通用模組 |
| 2. 設計 Schema | 把指標訂成結構化、可執行的規格 | 只需要「怎麼把指標寫成完整規格」的通用知識，不需要指標內容本身的領域判斷 | 可拆成 Skill，供③模組載入 |
| 3. 執行填報 | 照 Schema 比對輸出、產出報告 | 不需要——純粹是「照規格執行」，換任何領域都適用 | 值得拆成③能力模組，無領域判斷、跨領域可重用 |
| 4. 優化修正 | 判斷「這個落差該怎麼修」 | 需要——涉及對輸出內容本質的理解與取捨 | 呼叫方或人的職責，不該讓通用模組代為決策 |

**核心判斷**：四階段中只有「2. 設計」與「3. 執行」是無領域判斷、值得抽成可重用元件的部分；「1. 蒐集」與「4. 優化」本質上需要領域知識，勉強做成通用 Agent 只會產生看似客觀、實則各領域都不精準的判斷邏輯。

**建議的最小可行版本**：先做「2 + 3」——一個③能力模組（執行比對、產出報告），搭配一份 Skill（Schema 結構規範、判定分級規則）。指標規劃（1）留給呼叫方在調用時提供 Schema 內容，修正決策（4）留給呼叫方或人判讀報告後自行決定。等實際用出「同一種修正建議反覆出現、值得標準化」的證據，才評估要不要把 4 拆成獨立 Agent——這正是本文件第九節「何時該正式化協調層」同一套「先觀察痛點、後拆分」邏輯在評測情境下的應用。

**可追溯性是評測迴圈的前提，不是加分項**：任何「通過/不通過」的判定都必須能回指到目標輸出的具體依據（位置、數據、引用），否則評測本身就不可信、也無法作為第 4 階段優化的輸入——沒有依據的落差報告，等於沒有評測。
