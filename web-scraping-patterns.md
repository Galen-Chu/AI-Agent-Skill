---
name: web-scraping-patterns
description: 提供網路資料蒐集的判斷邏輯、結構化擷取規則、資料清理方式與版權合規規範。當需要決定用搜尋還是直接抓取、如何處理反爬蟲限制、或如何整理擷取到的網頁內容時使用此技能。
---

# 網路資料蒐集技能

這份技能提供「蒐集網路資料」的判斷邏輯，分成四部分：來源判斷、擷取規則、資料清理、版權合規。

## 一、來源判斷：搜尋 vs 直接抓取

1. **已知明確網址** → 直接抓取該頁面內容
2. **只有關鍵字或主題** → 先搜尋，從結果中挑選候選來源（優先原始來源，如官方網站、新聞社、學術資料庫，而非聚合型二手平台）
3. **需要多來源交叉比對**（例如查證同一事件的不同報導） → 搜尋後對前 3-5 個相關結果分別抓取，而非只採信第一個結果

## 二、結構化擷取規則

從網頁內容中優先擷取以下欄位（找不到則留空，不臆測填補）：
- 標題
- 發布/更新日期
- 作者或發布單位
- 主要內容（改寫摘要，非逐字複製）
- 原始網址

擷取時注意：
- 廣告、導覽列、留言區等非主體內容應排除，只取正文
- 若頁面是聚合/轉載內容，優先追溯到原始來源網址

## 三、資料清理常見問題

1. **HTML 殘留標籤或亂碼** → 清理後只保留純文字內容
2. **重複內容**（同一新聞被多個來源轉載） → 保留最早或最權威的來源，其餘標記為重複，不重複列入結果
3. **編碼問題**（中文亂碼、特殊符號跑版） → 若偵測到明顯亂碼，該筆資料標記「編碼異常」，不強行輸出破碎內容
4. **內容過短或抓取失敗**（頁面被防爬機制阻擋，只抓到空殼） → 標記為「無法完整擷取」，不用搜尋摘要（snippet）冒充完整內容

## 四、版權合規規範（務必遵守）

蒐集到的內容進入下游 Agent（摘要、分析、呈現）前，必須先符合以下原則，避免侵權風險擴散到整個工作流：

1. **改寫優先**：內容一律以自己的話重新摘要，不直接複製原文段落
2. **引用限制**：若確實需要保留原文用詞（例如關鍵數據、官方聲明的精確措辭），單次引用不超過 15 個字，且同一來源全篇僅能引用一次
3. **不做大量位移性摘要**：單一來源的摘要長度應遠短於原文，且用詞需大幅重組，不逐句對應原文順序
4. **來源標註**：不論是否引用原文，都應保留 `source_url`，讓下游 Agent 或使用者可自行查證原始內容
5. **歌詞/詩詞/受版權保護的創作內容**：一律不擷取重製，只能說明「該來源包含此類內容」並附上連結

## 五、輸出前檢查清單

- [ ] 每筆資料都有 `source_url` 可供查證
- [ ] 摘要內容已改寫，非逐字複製
- [ ] 若有引用，單一來源未超過一次、且在 15 字以內
- [ ] 已排除重複來源與抓取失敗的空殼資料
- [ ] 編碼異常或無法完整擷取的項目已明確標記，而非省略不提
